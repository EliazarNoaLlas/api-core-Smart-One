INSERT INTO core_user_types(id,
                            description,
                            code,
                            enable,
                            created_at)
VALUES (?, ?, ?, ?, ?);
